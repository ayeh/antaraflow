<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only. Android 9 and up refuse cleartext by default, which is
  right for anything that ships — but the emulator reaches a Herd site on the
  host through 10.0.2.2 over plain HTTP, so development would otherwise have
  no route to the API at all.

  Scoped to the loopback addresses rather than opened globally: 10.0.2.2 is
  the emulator's alias for the host machine, and localhost is a device on the
  end of `adb reverse`.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
